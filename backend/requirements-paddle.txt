# Optional: PaddleOCR PP-Structure (layout + table → HTML/TSV).
# Install in a dedicated venv. NumPy 1.x + Paddle 2.x stacks are less fragile than mixing with NumPy 2.
# See https://www.paddlepaddle.org.cn/install/quick
#
# Example (CPU):
#   pip install paddlepaddle==2.6.2 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/
#   pip install "paddleocr>=2.6.0.3,<2.8"
#
# Avoid paddleocr 3.x for this project unless we migrate to the new PaddleX API.

paddlepaddle>=2.6.0,<3.0
paddleocr>=2.6.0.3,<2.8
